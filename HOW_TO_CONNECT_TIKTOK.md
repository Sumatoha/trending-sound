# 🎯 Как подключить TikTok API - Пошагово

## Вариант 1: RapidAPI (проще всего)

### Шаг 1: Регистрация (2 минуты)

1. Идите на https://rapidapi.com/
2. Sign Up → укажите email
3. Подтвердите email

### Шаг 2: Найдите TikTok API (1 минута)

1. В поиске: "TikTok"
2. Выберите "TikTok API" от tikapi
3. Перейдите на страницу API

### Шаг 3: Получите API ключ (1 минута)

1. Нажмите "Subscribe to Test"
2. Выберите план:
   - **Free**: 50 запросов/мес (для теста)
   - **Basic**: 500 запросов/мес ($10)
   - **Pro**: 10,000 запросов/мес ($50)
3. Скопируйте **X-RapidAPI-Key** (справа на странице)

### Шаг 4: Добавьте ключ в .env

```bash
# .env
TELEGRAM_BOT_TOKEN=ваш_токен
DATA_DIR=./data
RAPIDAPI_KEY=ваш_rapidapi_ключ_здесь  # ← Добавить
```

### Шаг 5: Обновите код (5 минут)

Откройте `internal/parser/tiktok_api.go` и замените функцию:

**Найдите строку 44:**
```go
url := "https://m.tiktok.com/api/music/trending"
```

**Замените на:**
```go
url := "https://tiktok-api6.p.rapidapi.com/music/trending"
```

**Найдите строку 53-56 (headers), замените на:**
```go
req.Header.Set("X-RapidAPI-Key", os.Getenv("RAPIDAPI_KEY"))
req.Header.Set("X-RapidAPI-Host", "tiktok-api6.p.rapidapi.com")
```

**Найдите строку 106-110 (getMockData fallback), УДАЛИТЕ:**
```go
// УДАЛИТЬ ЭТИ СТРОКИ:
if len(sounds) == 0 {
    return p.getMockData(category), nil
}
```

### Шаг 6: Запустите и тестируйте

```bash
go run cmd/bot/main.go
```

В логах увидите:
```
Fetched 50 sounds for category: fitness  # ← Реальные данные!
```

Готово! Теперь бот использует **настоящие** трендовые звуки TikTok.

---

## Вариант 2: Без API ключа (бесплатно, но сложнее)

### Метод: Найти публичный endpoint через DevTools

**Шаг 1:** Откройте https://ads.tiktok.com/business/creativecenter/music в Chrome

**Шаг 2:** F12 → вкладка Network → фильтр XHR

**Шаг 3:** Обновите страницу, найдите запрос типа:
```
popular_trend/music/list
```

**Шаг 4:** Кликните на него, скопируйте:
- Request URL
- Request Headers

**Шаг 5:** Обновите `tiktok_api.go`:
```go
url := "СКОПИРОВАННЫЙ_URL"
req.Header.Set("User-Agent", "СКОПИРОВАННЫЙ_USER_AGENT")
req.Header.Set("Referer", "https://ads.tiktok.com/...")
```

**Минус:** Может перестать работать если TikTok изменит API.

---

## Что рекомендую?

**Для MVP теста:** Mock данные (уже работают)

**Для запуска с первыми юзерами:** RapidAPI Free (50 запросов/мес)

**Для масштаба:** RapidAPI Pro ($50/мес, 10k запросов)

---

## FAQ

**Q: Сколько это стоит?**
A: Free план - 0$. Базовый - $10/мес.

**Q: Достаточно ли Free плана?**
A: Для 10-20 пользователей - да. Для >100 - нет.

**Q: Когда нужно переходить с mock данных?**
A: Когда у вас будет >50 активных пользователей.

**Q: Что если RapidAPI API сломается?**
A: Верните getMockData() fallback в код.
