# TikTok Trending Sounds Tracker Bot

Telegram-бот для отслеживания трендовых звуков TikTok с автоматическими уведомлениями.

## Возможности

- 🎵 Автоматический парсинг трендовых звуков TikTok Creative Center
- 📊 Детектор трендов с расчетом роста использования
- 🔔 Автоматические алерты по выбранным нишам
- 💾 SQLite база данных с историей
- 🤖 Telegram бот с удобным интерфейсом
- ⏰ Автоматический сбор данных каждые 3 часа
- 🚀 Деплой на Railway с persistent storage

## Технологии

- **Go 1.21+**
- **SQLite** (go-sqlite3)
- **Telegram Bot API** (go-telegram-bot-api/v5)
- **Rod** (браузерная автоматизация)
- **Cron** (robfig/cron/v3)

## Структура проекта

```
trending-sound/
├── cmd/bot/main.go           # Entry point
├── internal/
│   ├── config/               # Конфигурация
│   ├── storage/              # База данных
│   ├── parser/               # Парсеры TikTok
│   ├── detector/             # Детектор трендов
│   ├── bot/                  # Telegram бот
│   └── scheduler/            # Cron задачи
├── migrations/               # SQL миграции
├── Dockerfile
├── railway.json
└── .env.example
```

## Установка и запуск

### Локально

1. Клонировать репозиторий:
```bash
git clone <repo-url>
cd trending-sound
```

2. Скопировать .env.example в .env и заполнить:
```bash
cp .env.example .env
```

3. Установить зависимости:
```bash
go mod download
```

4. Запустить:
```bash
go run cmd/bot/main.go
```

### Docker

```bash
docker build -t trending-sound-bot .
docker run -e TELEGRAM_BOT_TOKEN=your_token -v $(pwd)/data:/app/data trending-sound-bot
```

### Railway

1. Создать новый проект на Railway
2. Подключить GitHub репозиторий
3. Добавить переменную окружения: `TELEGRAM_BOT_TOKEN`
4. Создать Volume и примонтировать к `/app/data`
5. Деплой произойдет автоматически

## Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - (обязательно) |
| `DATA_DIR` | Директория для БД | `/app/data` |
| `LOG_LEVEL` | Уровень логирования | `info` |

## Команды бота

- `/start` - Начать работу и выбрать ниши
- `/niches` - Изменить подписки на ниши
- `/trending` - Показать текущие трендовые звуки

## Поддерживаемые ниши

- Fitness
- Beauty
- Comedy
- Business
- Tech
- Lifestyle
- Gaming

## Как работает детектор трендов

Звук считается трендовым, если:
- Количество использований: 500 - 30,000
- Рост использования за 24ч: >150%
- Входит в топ-10 по росту в своей нише

## Автоматизация

- **Сбор звуков**: каждые 3 часа
- **Отправка алертов**: каждые 6 часов
- **Fallback механизм**: автоматическое переключение на API при 3+ фейлах Rod парсера

## База данных

### Таблицы

**sounds**
- id, title, author, url, uses_count, category, created_at, updated_at

**sound_history**
- id, sound_id, uses_count, recorded_at

**users**
- id, telegram_id, niches, is_premium, created_at

## Разработка

### Обновление селекторов парсера

CSS селекторы в `internal/parser/tiktok_rod.go` могут требовать обновления при изменении структуры TikTok Creative Center.

### Тестирование парсера

```bash
# Запустить бота локально и проверить логи
go run cmd/bot/main.go
```

### Добавление новой ниши

1. Добавить в `parser.Categories` в `internal/parser/parser.go`
2. Добавить в `parser.CategoryDisplayNames`

## Лицензия

MIT

## Авторы

Ваше имя
